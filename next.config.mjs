/** @type {import('next').NextConfig} */
const nextConfig = {};
// next.config.js
module.exports = {
    images: {
      domains: ['raw.githubusercontent.com'], // Ajoutez raw.githubusercontent.com à la liste des domaines autorisés
    },
  };
  

export default nextConfig;
